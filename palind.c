#include <stdio.h>
#include <ctype.h>

bool is_palindrome(char *str)
{
    int len = strlen(str);
    for(int i=0;i<len/2;i++)
    {
        char to_lower=str[i];
        char to_lower=str(len-1-i)
    }
}